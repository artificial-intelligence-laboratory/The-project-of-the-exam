<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.dao.AdminRoleDao">

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        select id,role_id,admin_id,create_time,update_time,is_deleted
    </sql>

    <!--查询出用户的roleId-->
    <select id="findRoleByAdminId" resultType="long">
        select role_id
        from acl_admin_role
        where admin_id=#{adminId}
        and is_deleted =0
    </select>

    <!--根据adminId删除角色-->
    <update id="deleteByAdminId">
        update acl_admin_role set
        is_deleted=1
        where admin_id=#{adminId}
    </update>

    <!--插入角色-->
    <insert id="insert">
        insert into acl_admin_role (admin_id,role_id)
        values (#{adminId},#{roleId});
    </insert>



</mapper>